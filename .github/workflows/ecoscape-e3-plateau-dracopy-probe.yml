name: ECOSCAPE E3 PLATEAU DracoPy Probe
on:
  workflow_dispatch:
  push:
    branches: [research/ecoscape-moe-qa300-b13]
    paths:
      - research/ecoscape/plateau_full_domain/probe_dracopy.py
      - .github/workflows/ecoscape-e3-plateau-dracopy-probe.yml
permissions: {contents: read}
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install --disable-pip-version-check requests numpy DracoPy
      - run: python research/ecoscape/plateau_full_domain/probe_dracopy.py
      - uses: actions/upload-artifact@v4
        with:
          name: ECOSCAPE_PLATEAU_DRACOPY_PROBE_B84
          retention-days: 30
          path: output
