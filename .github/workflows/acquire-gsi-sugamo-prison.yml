name: Acquire GSI Sugamo Prison historical photos

on:
  workflow_dispatch:
  push:
    branches: [research/mlit-l03b-acquisition-20260811]
    paths:
      - '.github/workflows/acquire-gsi-sugamo-prison.yml'
      - 'research/acquire_gsi_sugamo_prison.py'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/acquire-gsi-sugamo-prison.yml'
      - 'research/acquire_gsi_sugamo_prison.py'

permissions:
  contents: read

jobs:
  acquire:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Playwright
        run: |
          python -m pip install --disable-pip-version-check playwright
          python -m playwright install --with-deps chromium
      - name: Search and acquire historical photos
        continue-on-error: true
        run: python research/acquire_gsi_sugamo_prison.py
      - name: Publish summary
        if: always()
        shell: bash
        run: |
          echo '## GSI Sugamo Prison historical-photo acquisition' >> "$GITHUB_STEP_SUMMARY"
          if [ -f out-gsi-sugamo-prison/SUMMARY.json ]; then
            cat out-gsi-sugamo-prison/SUMMARY.json >> "$GITHUB_STEP_SUMMARY"
          else
            echo 'SUMMARY.json not produced' >> "$GITHUB_STEP_SUMMARY"
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: gsi-sugamo-prison-historical-photos
          path: out-gsi-sugamo-prison
          if-no-files-found: warn
          retention-days: 14
